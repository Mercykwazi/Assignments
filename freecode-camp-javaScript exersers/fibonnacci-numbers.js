function sumFibs(num) { 
   var prevNumber = 0;
    var currentNum = 1;
        var result = 0;
         while(currentNum <= num) {
         }
            if (currentNum % 2 !== 0) {
            result += currentNum;
        }
         currentNum = currentNum+prevNumber; 
}